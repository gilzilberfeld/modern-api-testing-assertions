# Round 7 Prompt

I have a bookstore review API with in-memory storage (no database). I can test everything that's running right now.

What would you test to verify long-term reliability — the scenarios that can't be covered with simple HTTP calls against a running server? List them with enough detail to write the tests if the infrastructure existed.
